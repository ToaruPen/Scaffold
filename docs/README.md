# docs/

Repository-level documentation for Scaffold itself.

## Scope

- Design notes and decisions for maintaining this source repository
- Internal planning and architecture notes

Distributable contract docs for consumer repositories live under:

- `framework/docs/contract/`

Repository quality/lint settings are managed from root config files:

- `pyproject.toml`
- `.pre-commit-config.yaml`
- `Makefile`
